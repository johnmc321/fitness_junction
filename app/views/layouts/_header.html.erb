<header class="navbar navbar-fixed-top navbar">
  <div class="navbar-inner">
    <div class="container">
      <% if signed_in? %>
      <%= link_to "Fitness Junction", current_user, id: "logo" %>
      <% else %>
      <%= link_to "Fitness Junction", root_path, id: "logo" %>
      <% end %>
      <nav>


        <ul class="nav pull-right">
        <% if signed_in? %>
          <li><%= link_to "Home", current_user %></li>
          <% else %>
          <li><%= link_to "Home", root_path %></li>
          <% end %>
          <li><%= link_to "About ", about_path %></li>
          <% if signed_in? %>
          <li><%= link_to "Search", search_path %></li>
          <li><%= link_to "Users", users_path %></li>
          
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                 <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>

            </li>
             <%= link_to "Upload", new_upload_path, class: "btn btn-small btn-warning" %>
          <% else %>
          
          <%= link_to " Sign In", signin_path , class: "btn btn-small btn-primary" %>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>

